import React, { Component } from 'react';
import Feeds from '../Feeds/Feeds';

class Main extends Component {
  render() {
    return (
      <div>
        <Feeds />
      </div>
    );
  }
}

export default Main;
